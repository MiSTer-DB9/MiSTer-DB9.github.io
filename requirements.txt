# Build dependencies for the MiSTer-DB9 site.
# Install into a venv:  python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
mkdocs-material>=9.5,<10
mkdocs-minify-plugin>=0.8,<1
